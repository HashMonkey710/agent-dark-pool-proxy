{
  "name": "agent-dark-pool-proxy",
  "version": "1.0.0",
  "description": "Payment proxy for Agent Dark Pool - Private MEV protection for x402 agents",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=bun",
    "start": "bun run dist/index.js",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@lucid-agents/agent-kit": "^1.1.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  }
}
